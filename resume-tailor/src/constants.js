/**
 * Shared defaults for resume-tailor networking helpers.
 */

/** Default timeout for API requests (ms); use from fetch wrappers so behavior stays consistent. */
export const DEFAULT_API_TIMEOUT_MS = 30_000;
